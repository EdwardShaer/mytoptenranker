<!DOCTYPE html>
<html>
  <head>
    <title>MyTop10</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <div class='topnav'>
      <a class="logo" href='<%=user_url(current_user)%>'> <%=  image_tag("logos/mytop10.png", size: '60') %> </a>
      <a class = 'active' href='/'>Home</a>
      <a href='<%=rankings_url%>'>New Ranking</a>
      <a href='<%=new_suggestion_url%>'>Suggestion</a>
    </div>
  </head>

  <body class='body'>
    <%= yield %>
  </body>
</html>
